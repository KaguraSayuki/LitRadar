# 更新记录

## 未发布

网页配置与自定义 AI 接入。此前的 0.2.0 发布已撤回，以下为开发中的功能记录。

公开研究模板已清空，演示与邮件测试改用合成数据，并清理了相关历史内容。

### 新增与改进

- **网页配置**：通过首次设置引导和分类表单管理研究方向、数据源、邮箱、模型、阅读
  偏好与自动更新；支持结构化检索条件、结果预览、连接测试和邮件文件导入。
- **自定义模型服务**：接入兼容 OpenAI Chat Completions 的 API，自定义地址、模型名
  和密钥；支持获取模型列表、手动填写模型、兼容性测试和常见参数的自动适配。
- **操作授权**：在网页内输入密码后，设置与受保护的运行操作共用固定 5 分钟授权。
  可以主动退出，到期保留未保存的输入；同一浏览器的标签页同步状态，其他浏览器
  和设备独立授权。阅读登录与操作授权分别管理。
- **后台运行与进度**：手动流水线显示阶段、处理数量和耗时，刷新或离开页面后可
  继续查看；重复提交不会重复启动，部分失败和进程中断会明确显示。
- **每日自动更新**：在网页选择时间、时区和研究方向，由独立调度进程执行；持久记录
  当天任务，与网页、命令行共用流水线锁及次数限制。
- **界面与手机操作**：统一浅色和深色主题、表单布局、保存区域与按钮尺寸，调整手机
  导航及触控区域。
- **设置与凭据管理**：设置保存检查版本、原子写入并保留最近五份备份；凭据独立管理，
  新任务读取最新凭据，正在运行的任务保留启动时的快照。

### 兼容与升级

- 旧 DeepSeek 配置、凭据名称、单研究方向及同步运行接口继续兼容。模型列表接口
  不可用时可手动填写模型；模型接入目前以 Chat Completions 协议为兼容范围。
- 升级前备份数据库、设置、研究方向及私有凭据，重新安装项目依赖并重启服务。
  数据库连接时自动迁移到 schema 8；回退时应使用匹配的代码和数据库备份。
- 推荐使用 `litradar serve` 启动网页与调度进程。启用网页计划前，维护者须停用旧的
  外部定时入口，再执行 `litradar schedule-handoff --external-timers-stopped` 确认接管。
  升级不会自动停用系统定时任务，也不会自动开启新计划。
- 由服务环境变量注入的凭据仍由部署层管理；希望在网页更换密钥时，按部署指南迁移
  原有的 `.env` 环境注入方式。
- 页面关闭或操作授权到期不会取消已启动的后台任务；服务器进程重启会中断任务，
  不会自动重试。

操作说明见 [网页设置指南](docs/web-settings.md)，完整升级、调度接管和恢复步骤见
[部署与维护](docs/deployment.md)。
