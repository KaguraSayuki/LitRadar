# 贡献指南

欢迎修复问题、改善阅读体验或增加数据来源。请先阅读 [README](README.md) 了解产品，
再参考[开发导览](docs/architecture.md)找到对应模块。

## 建立开发环境

需要 Python 3.11+。在项目目录创建虚拟环境并安装项目和测试工具：

```bash
python3 -m venv .venv
.venv/bin/python -m pip install -e . pytest
.venv/bin/python -m pytest -q
```

Windows 使用 `.venv\Scripts\python.exe`。部分前端逻辑测试需要 Node.js 18+；缺少 Node
时会跳过这些测试。项目没有前端打包步骤，页面由 Jinja2 模板和原生 JavaScript 构成。

开发预览请指定临时配置、临时数据库和合成文献。测试替换外部服务，不应使用真实密钥
调用模型或访问个人邮箱。运行实例与测试环境应使用不同的数据目录。

## 修改与验证

先检查现有实现和测试，保持改动围绕一个完整的用户问题。验证范围与风险匹配：

- 评分和摘要：检查重复调用、部分失败、已有结果保留以及跨方向隔离。
- 数据结构：同时修改新库结构与升级迁移，验证旧数据保留。
- 网页：检查真实表单提交、权限、窄屏布局和触控操作。
- 文档：确认步骤可执行、链接有效，并区分保存设置与开始运行。

在 PR 中说明问题、修改后的行为和实际完成的验证。未验证的部署环境要写清楚。
回归测试应覆盖会发生的故障，不为单纯的文字或样式修改堆积测试。
同一行为优先在最合适的一层验证；网页只补充路由、权限和交互差异。参数化选择有区别的
边界场景，避免把相同逻辑在所有页面和状态上全组合展开。连续操作可合并为一个完整场景。

## 代码与产品约定

普通用户具备研究背景，但不预设编程能力。日常操作应通过表单、选择器、合理默认值和
可操作的错误提示完成。安装和宿主机维护可以交给维护者；运行参数和内部存储细节放在
开发或部署资料中。

新增来源应复用统一的题录清洗和入库入口。读取或更新方向相关状态时，必须带方向标识。
耗时网络请求与数据库事务分开；部分失败应保留成功工作，并在进度和退出状态中如实反映。

## 提交与评审

分支名不要使用 `codex/` 前缀。提交标题使用英文 Conventional Commit 风格：

```text
feat(rank): score new papers and reuse previous results
```

实质性修改附正文，说明原问题、修改后的行为、必要取舍和验证。
标题以简洁的英文祈使句描述变化；类型可用 `feat`、`fix`、`refactor`、`docs` 或 `test`，
组件范围按需填写。不要用文件清单或对话过程代替说明。

一条提交围绕一项连贯改动，相关实现、测试和文档可以放在一起。不要只按前后端或审查
轮次拆分。提交前检查完整 diff，使用账号的 GitHub 隐私邮箱；不要擅自重写共享主分支历史。

## 保护私人数据

所有公开示例、测试和截图使用空模板或从零构造的合成数据。真实研究方向、检索词、
期刊偏好、作者、种子论文、订阅邮件、凭据和数据库都不得进入版本库或公开讨论。
问题报告应删去这些内容，邮件样例不能沿用真实邮件的论文组合和追踪链接。

提交、PR 和发布前检查实际要分享的内容及相关历史，而不只检查文件是否被忽略。
发现泄露时私下联系维护者，避免在公开 issue 中再次粘贴原文。
